name: Secret Guard

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check tracked files for Supabase admin secrets
        run: python scripts/check_no_supabase_admin_secrets.py
