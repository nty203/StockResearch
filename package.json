{
  "name": "stock-research",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "pnpm --filter @stock/web dev",
    "build": "pnpm --filter @stock/web build",
    "lint": "pnpm --filter @stock/web lint",
    "typecheck": "pnpm --filter @stock/web typecheck"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=10.0.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild", "sharp", "unrs-resolver", "workerd"]
  }
}
